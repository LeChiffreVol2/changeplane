# ChangePlane task loop for an existing coding agent

The maintained consumer instructions now live in [skills/changeplane/SKILL.md](../skills/changeplane/SKILL.md). Install that whole skill directory through the repository's normal configuration review, preserving existing instructions.

Read its **Continue configured parallel work** section only after completing [operator setup](../docs/team-operator.md). Keep the operator credentials outside the coding sandbox. Retain each task's assigned workspace across feedback and restarts; receipt is not completion.

This path remains an entry point for existing documentation links. The skill does not create credential isolation, launch a model or grant source-write, Guard, approval or merge authority.
