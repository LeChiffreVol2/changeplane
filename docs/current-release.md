# Current controlled-canary release

This is engineering evidence for an owner canary, not customer-launch approval. The live behavioral qualification used protected source `db4410979ec8c97ec87d92fb7e43f1a43fe56703` (managed v15), following [PR50](https://github.com/LeChiffreVol2/changeplane/pull/50), [PR51](https://github.com/LeChiffreVol2/changeplane/pull/51) and [PR53](https://github.com/LeChiffreVol2/changeplane/pull/53). The [bounded observation record](../evidence/changeplane-controlled-canary-observation.json) preserves failures as well as successful retests.

The first organization canary proved authenticated Verify onboarding, one nine-file setup PR, separate Guard publication through the deployed journal, and strict no-bypass Rulesets bound to both behavioral CI and Guard publishers. A deliberately failing source change was blocked; a corrected new head obtained fresh PASS. The owner observed GitHub web inbox failure notifications. These are synthetic observations, not customer outcomes or email-delivery proof.

Live same-SHA testing found two issues: status-only restart retained old success, and explicitly clearing terminal fields still failed to begin a new evaluation. The second attempt did retire the old PASS safely. Affected enrollments were halted and their occupied authority retained. Managed v15 instead keeps the same Check blocked with `action_required` while a new generation evaluates. Live run `34275114616`, attempt 2, passed: Check `102226179816` moved from PASS to blocked evaluation and then to fresh PASS on the same commit; Strict Head returned `CLEAN`. A subsequent new commit started with no inherited Guard. A small follow-up explicitly sets the completion timestamp because GitHub retained the blocked begin timestamp after fresh success.

[Candidate CI](https://github.com/LeChiffreVol2/changeplane/actions/runs/34274301886) and [protected-main CI](https://github.com/LeChiffreVol2/changeplane/actions/runs/34274671469) passed: 481 unit/integration tests, 14 Guard PostgreSQL checks, 14 pilot PostgreSQL checks, 6 TLS checks, 13 Chromium journeys, build and audits. The Git deployment was rebuilt to receive its exact-release environment binding; there was no local source upload. Signed-out UI/API smoke and the twelve-case/six-assertion synthetic Origin contract passed; browser console errors were zero.

The stalled-run exercise is in progress. Its exact owning workflow was cancelled after Guard begin at `2026-09-08T20:32:28Z`; the new head remains blocked. Scheduled recovery timing and final temporary-resource cleanup are still pending. The installed workflow turns `ACTION_REQUIRED` into a failed job for GitHub notifications. A five-minute cron declaration does not guarantee recovery within ten minutes.

Readiness reports separate Guard identity and valid journal configuration; it does not query enrollment, database health or durability. `guardPublicationSerialized` and `commercialReady` remain false. The budget remains USD 0 now, with USD 100 reserved for a later scale decision. Retain only the approved isolated Free Guard database and leave other projects unchanged. Repair and customer/paid access remain closed. Eligible commercial hosting, effective legal terms, provider authority recovery, design partners, real payments and a complete thirty-day reliability/value cohort remain external launch gates.
