# Current controlled-canary release

This is a dated engineering observation, not customer-launch approval. The protected Production source is `42de0baf45474582c9e0e1e61bc53ff1221fb8f1` (managed v14), following [PR50](https://github.com/LeChiffreVol2/changeplane/pull/50) and [PR51](https://github.com/LeChiffreVol2/changeplane/pull/51). See the [bounded observation record](../evidence/changeplane-controlled-canary-observation.json).

The first organization-owned canary proved authenticated Verify onboarding, one nine-file setup PR, separate Guard publication through the deployed journal, and an active strict no-bypass Ruleset binding both behavioral CI and Guard publishers. A deliberately failing source change was blocked; a corrected new head obtained fresh PASS. GitHub web inbox notifications arrived for CI, Guard, rerun and deliberately denied manual reconciliation failures. These are synthetic owner observations, not customer outcomes or email-delivery proof.

The same-SHA rerun failed qualification: GitHub retained the earlier success and timestamps after a status-only restart. The PR was made draft, its enrollment disabled and occupied journal authority retained. The original disposable repository was deleted after owner reauthentication; GitHub also removed its two organization App installations. A replacement synthetic repository has a reviewed, CI-protected setup. Its Guard installation and the final repository/organization cleanup remain outstanding.

PR51 retires a usable old conclusion to `action_required` before reopening the stable Check, explicitly clears terminal fields and refreshes the start time. Regression tests reproduce retained fields and a refused restart that must leave the old result blocked. [Candidate CI](https://github.com/LeChiffreVol2/changeplane/actions/runs/34269759177) and [protected-main CI](https://github.com/LeChiffreVol2/changeplane/actions/runs/34270122196) passed: 478 unit/integration tests, 14 Guard PostgreSQL checks, 14 pilot PostgreSQL checks, 6 TLS checks, 13 Chromium journeys, build and audits. Live same-SHA validation of this fix remains pending.

The deployment was rebuilt from the protected Git deployment to receive its exact-release environment binding; it was not a local source upload. Signed-out UI/API smoke and the twelve-case/six-assertion synthetic Origin contract passed without external Origin requests. Readiness reports separate Guard identity and valid journal configuration, while `guardPublicationSerialized` and `commercialReady` remain false. Readiness does not query enrollment, database health or durability.

Scheduled recovery still needs a live stalled-Guard and timing exercise. The installed workflow already turns the Action's `ACTION_REQUIRED` output into a failed job through its subsequent notification step; a successful helper return alone does not describe the whole workflow. The five-minute schedule is best effort, not a ten-minute delivery guarantee.

The current budget remains USD 0, with USD 100 reserved for a later scale decision. Keep the isolated Free Guard database as approved and leave other database projects unchanged. Repair and customer/paid access remain closed. Commercial hosting eligibility, effective legal terms, provider authority recovery, design partners, real payments and a complete thirty-day reliability/value cohort remain separate launch gates. A free engineering closeout still requires the pending live checks and deletion of the temporary GitHub resources.
